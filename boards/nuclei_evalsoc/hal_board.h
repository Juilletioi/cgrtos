/**
 * @file boards/nuclei_evalsoc/hal_board.h
 * @brief Nuclei evalsoc 板级配置入口（可移植 BSP 布局）
 * @author Cong Zhou / Juilletioi
 * @version 5.1.0
 * @date 2026-07-22
 * @copyright CG-RTOS
 *
 * @details
 * 换板时：新建 `boards/<board>/hal_board.h`，Makefile 设 `BOARD=<board>`。
 * 当前内容委托既有 `hal/hal_board.h` MMIO 表，避免双份漂移。
 */
#ifndef BOARD_NUCLEI_EVALSOC_HAL_BOARD_H
#define BOARD_NUCLEI_EVALSOC_HAL_BOARD_H
#include "../../hal/hal_board.h"
#endif
